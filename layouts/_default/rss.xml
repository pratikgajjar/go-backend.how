{{- $pages := .RegularPages -}}
{{- if .IsHome -}}
  {{- $pages = where site.RegularPages "Section" "posts" -}}
{{- end -}}
{{- $limit := site.Config.Services.RSS.Limit -}}
{{- if and (gt $limit 0) (gt (len $pages) $limit) -}}
  {{- $pages = first $limit $pages -}}
{{- end -}}
{{- $title := site.Title -}}
{{- if site.Params.title -}}
  {{- $title = site.Params.title -}}
{{- end -}}
{{- if not .IsHome -}}
  {{- $title = printf "%s — %s" $title .Title -}}
{{- end -}}
{{- $description := site.Params.description | default "Recent posts" -}}
{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ $title }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ $description }}</description>
    <generator>Hugo</generator>
    <language>{{ site.Language.LanguageCode }}</language>
    {{- with site.Params.author.email }}
    <managingEditor>{{ . }}{{ with site.Params.author.name }} ({{ . }}){{ end }}</managingEditor>
    <webMaster>{{ . }}{{ with site.Params.author.name }} ({{ . }}){{ end }}</webMaster>
    {{- end }}
    {{- with site.Copyright }}
    <copyright>{{ . }}</copyright>
    {{- end }}
    {{- /* lastBuildDate: use the most recent post's date, not the home
           page's stale 2023 .Date. Falls back to .Date if no posts. */ -}}
    {{- $latest := index $pages 0 -}}
    {{- $latestDate := .Date -}}
    {{- if and $latest (not $latest.Date.IsZero) }}{{ $latestDate = $latest.Date }}{{ end -}}
    {{- if not $latestDate.IsZero }}
    <lastBuildDate>{{ $latestDate.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{- end }}
    {{- with .OutputFormats.Get "RSS" }}
    {{- printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end }}
    {{- range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{- with site.Params.author.email }}
      <author>{{ . }}{{ with site.Params.author.name }} ({{ . }}){{ end }}</author>
      {{- end }}
      <guid>{{ .Permalink }}</guid>
      {{- with .Description }}
      <description>{{ . | transform.XMLEscape | safeHTML }}</description>
      {{- else }}
      <description>{{ .Summary | transform.XMLEscape | safeHTML }}</description>
      {{- end }}
    </item>
    {{- end }}
  </channel>
</rss>
